# Flask Configuration
FLASK_SECRET_KEY=agribridge_super_secret_key_2026_demo

# Weather: Uses Open-Meteo API (https://open-meteo.com) — no API key required.
# If the API is unreachable (offline), the app automatically falls back to Demo Mode.
